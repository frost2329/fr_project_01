export function getProfile (state) {
    return state.profileState.profile;
}
export function getPosts (state) {
    return state.profileState.posts;
}
export function getUserStatus (state) {
    return state.profileState.userStatus;
}
